'use client';

import { IEvent } from '~/lib/database/models/event.models';

export default function Checkout({
  event,
  userId,
}: {
  event: IEvent;
  userId: string;
}) {
  return <form></form>;
}
